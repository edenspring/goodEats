extends layout.pug

include utils.pug

block content
  h3=`Would you like to remove ${recipe.dataValues.name} from ${box.name}?`
  form(action=`/boxes/${boxId}/remove/${recipeId}` method='post')
    button(type='submit' class='btn') Confirm
    a(href=`/boxes/${boxId}/edit`) Cancel
